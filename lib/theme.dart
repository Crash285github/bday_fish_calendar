import "package:flutter/material.dart";

final ThemeData appTheme = ThemeData.from(
  colorScheme: ColorScheme.fromSeed(
    seedColor: const Color(0xffaf01fa),
    brightness: Brightness.dark,
  ),
);
